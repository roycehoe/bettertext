version: '3.8'
services:
  database:
    image: mongo
    restart: always

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    depends_on:
      - database
    volumes:
      - /app/node_modules
      - ./backend:/app
  webpage:
    build: ./frontend
    volumes:
      - ./frontend/nginx/default.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "80:80"
